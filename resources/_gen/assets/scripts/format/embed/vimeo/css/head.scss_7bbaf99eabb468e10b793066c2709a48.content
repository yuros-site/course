#format-vimeo {
  overflow: hidden; }
  #format-vimeo #covers.hide, #format-vimeo #player.hide {
    top: 0;
    left: 0;
    opacity: 0;
    z-index: -1;
    position: absolute; }
  #format-vimeo .overlay {
    position: relative; }
    #format-vimeo .overlay img {
      width: 100%;
      height: 100%;
      display: block;
      object-fit: cover;
      position: relative;
      aspect-ratio: 16 / 9;
      object-position: center; }
    #format-vimeo .overlay .action {
      position: absolute; }
  #format-vimeo #player {
    width: 100%;
    overflow: hidden;
    position: relative;
    padding-top: 56.25%;
    /* 16:9 Aspect Ratio (divide 9 by 16 = 0.5625) */ }
    #format-vimeo #player iframe {
      top: 0;
      left: 0;
      bottom: 0;
      right: 0;
      width: 100%;
      height: 100%;
      position: absolute; }
  #format-vimeo .inform {
    text-wrap: nowrap; }
    #format-vimeo .inform h1 {
      margin-bottom: 1.5rem; }
    #format-vimeo .inform .plat {
      margin-left: 1rem; }
    #format-vimeo .inform .ml-a {
      margin-left: auto; }
    #format-vimeo .inform .icon {
      padding: .05rem; }
  #format-vimeo .action .icon {
    min-width: 3.85rem;
    min-height: 3.1rem;
    background-color: black; }
  #format-vimeo .action svg {
    min-width: 1.75rem;
    min-height: 1.75rem; }
  #format-vimeo .overlay:hover .action .icon {
    z-index: 15;
    color: white;
    transition: 150ms;
    border: 1px solid transparent;
    background-color: #eb2525; }

@media only screen and (min-width: 480px) {
  #format-vimeo .action .icon {
    min-width: 5rem;
    min-height: 4rem; }
  #format-vimeo .action svg {
    min-width: 2rem;
    min-height: 2rem; } }

@media only screen and (min-width: 1280px) {
  #format-vimeo .action .icon {
    min-width: 5rem;
    min-height: 4rem; }
  #format-vimeo .action svg {
    min-width: 2rem;
    min-height: 2rem; } }
